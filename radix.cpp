#include 'iostream'
#include 'math.h'
using namespace std;
int radix(int x,long y,int m,int n);
int min(int a, int b);
int main()
{
    int N1,N2;
    int tag,rad;
    cin>>N1>>N2>>tag>>rad>>endl;
    radix(N1,N2,tag,rad);
    //while(1);
}
int radix(N1,N2,tag,rad)
{
    int rad2==min(N1,N2)+1;
    for(rad2<='z')
    {
        if tag==1
        {
            //求余转换10进制比较
        }
        else if
        {

        }

    }
}